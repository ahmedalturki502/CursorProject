{
  // Database connection string configuration
  "ConnectionStrings": {
    // SQL Server Express connection string for development
    // Uses SQL Server Authentication with SA account
    "DefaultConnection": "server=localhost\\SQLEXPRESS;database=ECommerceDb;user id=sa;password=P@ssw0rd@123;TrustServerCertificate=True;MultipleActiveResultSets=true"
  },
  // JWT (JSON Web Token) configuration for authentication
  "Jwt": {
    // Secret key used to sign and verify JWT tokens (should be at least 32 characters)
    "Key": "YourSuperSecretKeyHereThatIsAtLeast32CharactersLong",
    // The issuer of the JWT token (this application)
    "Issuer": "ECommerceAPI",
    // The intended audience of the JWT token (client applications)
    "Audience": "ECommerceClient"
  },
  // Logging configuration for the application
  "Logging": {
    "LogLevel": {
      // Default log level for all components
      "Default": "Information",
      // Specific log level for ASP.NET Core framework components
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // Host configuration - allows all hosts to access the application
  "AllowedHosts": "*"
}
